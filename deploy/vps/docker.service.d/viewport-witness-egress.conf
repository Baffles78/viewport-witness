[Service]
# Docker can rewrite DOCKER-USER when its daemon restarts. Reassert this
# service's source-subnet rule immediately after every successful start.
ExecStartPost=/usr/local/sbin/viewport-witness-egress
